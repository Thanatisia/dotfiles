: "
All non-distro-specific Aliases
"

# Essentials
    alias dir="dir --all --color -sh"
    alias ls="ls --all --color -sh" 
# XOrg
    # alias startx="startx ~/.xinitrc"
# Dotfiles
    alias reload="source ~/.bashrc"
# Pacman
    alias paccheck="pacman -Qq"	# List all installed packages
    alias paccheck_full="pacman -Qi" # Show all fulled details of installed packages
    alias pacsearch="sudo pacman -Ss"	# Search for package not installed in th ArchLinux repositories
    alias pacinstall="sudo pacman -S"
    alias pacremove="sudo pacman -Rsu"
    alias pacupdate="sudo pacman -Sy"
    alias pacupgrade="sudo pacman -Su"
    alias pacupdg="sudo pacman -Syu"
    alias paclf="pacman -Ql "
